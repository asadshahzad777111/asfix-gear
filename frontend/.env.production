# Public Render API — baked into the Vite production build (Vercel / Pages).
# Not a secret. Override in Vercel Project Settings if the API host changes.
VITE_API_BASE=https://asfix-gear.onrender.com/api
